{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Portal Prácticas TI</title>
  <link rel="stylesheet" href="{% static 'portal/style3.css' %}">
  <style>
    /* Estilos para el menú hamburguesa */
    .hamburger {
      display: none;
      flex-direction: column;
      background: none;
      border: none;
      cursor: pointer;
      padding: 5px;
      z-index: 1001;
    }

    .hamburger span {
      width: 25px;
      height: 3px;
      background-color: white;
      margin: 3px 0;
      transition: all 0.3s ease;
      border-radius: 2px;
    }

    .hamburger.active span:nth-child(1) {
      transform: rotate(45deg) translate(7px, 7px);
    }

    .hamburger.active span:nth-child(2) {
      opacity: 0;
    }

    .hamburger.active span:nth-child(3) {
      transform: rotate(-45deg) translate(7px, -7px);
    }

    @media (max-width: 768px) {
      .header {
        padding: 15px 20px;
      }

      .hamburger {
        display: flex;
      }

      .nav {
        position: fixed;
        top: 0;
        right: -100%;
        width: 280px;
        height: 100vh;
        background: linear-gradient(135deg, #004aad 0%, #0066cc 100%);
        flex-direction: column;
        justify-content: flex-start;
        align-items: stretch;
        padding: 80px 0 20px;
        box-shadow: -5px 0 15px rgba(0,0,0,0.3);
        transition: right 0.3s ease;
        z-index: 1000;
        gap: 0;
      }

      .nav.active {
        right: 0;
      }

      .nav li {
        margin: 0;
        border-bottom: 1px solid rgba(255,255,255,0.1);
      }

      .nav li a {
        display: block;
        padding: 18px 25px;
        font-size: 16px;
        transition: all 0.2s;
      }

      .nav li a:hover {
        background: rgba(255,255,255,0.1);
        padding-left: 35px;
      }

      .section {
        padding: 20px 15px;
      }

      .login-container {
        padding: 20px;
        margin: 0 auto;
        max-width: 100%;
      }

      .login-container h2 {
        font-size: 1.5rem;
        margin-bottom: 20px;
      }

      .login-container input[type="text"],
      .login-container input[type="email"],
      .login-container input[type="password"] {
        font-size: 16px;
      }

      .login-container button {
        width: 100%;
        padding: 14px;
        font-size: 16px;
      }
    }
  </style>
</head>
<body>

  {% include 'portal/partials/_header_publico.html' %}

  <!-- Contenedor de login -->
  <section class="section">
    <div class="form-grid login-container">
      <h2>Iniciar Sesión</h2>
      
      {% if messages %}
        {% for msg in messages %}
          {% if "sesión" in msg|stringformat:"s" %}
            <p style="color:green;">{{ msg }}</p>
          {% endif %}
        {% endfor %}
      {% endif %}
      
      <form method="post">
        {% csrf_token %}
        {{ form.non_field_errors }}

        <label for="id_username">Correo:</label>
        {{ form.username }}
        {% if form.username.errors %}
          <div class="error" style="color:red;">{{ form.username.errors }}</div>
        {% endif %}

        <label for="id_password">Contraseña:</label>
        {{ form.password }}
        {% if form.password.errors %}
          <div class="error" style="color:red;">{{ form.password.errors }}</div>
        {% endif %}

        <button type="submit">Ingresar</button>
      </form>

      <p style="margin-top:15px; text-align:center;">
        ¿No tienes cuenta? 
        <a href="{% url 'registro' %}" style="color:var(--blue-600); font-weight:600;">Regístrate aquí</a>
      </p>
    </div>
  </section>
  {% include 'portal/partials/_footer.html' %}

</body>
</html>
